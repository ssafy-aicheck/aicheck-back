server:
  port: 0

spring:
  application:
    name: batch

  datasource:
    url: jdbc:mysql://aicheck-mysql:3306/batch?serverTimezone=UTC&characterEncoding=UTF-8

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: none
    database-platform: org.hibernate.dialect.MySQLDialect