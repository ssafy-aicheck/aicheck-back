eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}:${vcap.application.instance_id:${HOSTNAME}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://discovery:8761/eureka/